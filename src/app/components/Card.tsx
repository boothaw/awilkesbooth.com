interface NavLink {
  id: number;
  name: string;
  path: string;
}

const Card = () => {
  const pathname = usePathname();

  return (
    <div className="card">
                    
    </div>
  );
};

export default Card;
